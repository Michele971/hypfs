import os

APP_NAME = os.environ['APP_NAME']

LOCAL_HOST = os.environ['LOCAL_HOST']
INIT_PORT = int(os.environ['INIT_PORT'])
HOP_SERVER_PORT = int(os.environ['HOP_SERVER_PORT'])

HYPERCUBE_SIZE = int(os.environ['HYPERCUBE_SIZE'])
SUPERSET_THRESHOLD = int(os.environ['SUPERSET_THRESHOLD'])
